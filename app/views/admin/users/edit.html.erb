      <div id="main">
				<div class="dashboard-section">
					<div class="dashboard-container">
						<div class="dashboard-title">
							<h2>Edit <%= current_user.name %> Profile</h2>
						</div>
            <%= render 'form' %>
            <div class="card-body text-start pt-3">
		          <%= link_to "Back", admin_profile_path, class: "btn btn-outline-primary", role: "button" %>
		        </div>
					</div>
				</div>
			</div>
