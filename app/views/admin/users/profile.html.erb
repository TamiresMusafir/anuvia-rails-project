      <div id="main">
				<div class="dashboard-section">
					<div class="dashboard-container">
						<div class="dashboard-title">
							<h2><%= current_user.name %> Profile</h2>
						</div>
            <% if flash[:alert] %>
              <p class="alert alert-danger"><%= flash[:alert] %></p>
            <% end %>
            <div class="dashboard-body">
              <div class="row align-items-center">

                <!-- Avatar -->
                <div class="col-md-3 mb-3">
                  <% if @user.image.attached? %>
                    <%= image_tag @user.image,
                        class: 'img-fluid',
                        style: "object-fit: cover;" %>
                  <% else %>
                    <div class="rounded-circle bg-light d-flex align-items-center justify-content-center"
                         style="width:140px;height:140px;">
                      <span class="text-muted">No Image. Edit Profile and Put an Image</span>
                    </div>
                  <% end %>
                </div>
                <div class="col-md-9">
                  <div class="profile-info">
                    <div class="row mb-3">
                      <div class="col-sm-4">
                        <h4 class="team-title">Name:</h4>
                      </div>
                      <div class="col-sm-8">
                        <h5><%= @user.name %></h5>
                      </div>
                    </div>

                    <div class="row mb-3">
                      <div class="col-sm-4">
                        <h4 class="team-title">Email:</h4>
                      </div>
                      <div class="col-sm-8">
                        <h5><%= @user.email %></h5>
                      </div>
                    </div>

                    <div class="row">
                      <div class="col-sm-4">
                        <h4 class="team-title">Member Since:</h4>
                      </div>
                      <div class="col-sm-8">
                        <h5><%= @user.created_at.strftime("%B %Y") %></h5>
                      </div>
                    </div>
                  </div>
                </div>
                <div class="card-body text-center pt-3">
                  <%= link_to 'Update Profile', admin_edit_profile_path, class: "btn btn-secondary", role: "button" %>
                </div>
              </div>
            </div>
					</div>
				</div>
			</div>
